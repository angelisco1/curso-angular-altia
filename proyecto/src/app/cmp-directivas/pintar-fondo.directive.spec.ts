import { PintarFondoDirective } from './pintar-fondo.directive';

describe('PintarFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new PintarFondoDirective();
    expect(directive).toBeTruthy();
  });
});
