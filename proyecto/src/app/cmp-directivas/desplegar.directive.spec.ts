import { DesplegarDirective } from './desplegar.directive';

describe('DesplegarDirective', () => {
  it('should create an instance', () => {
    const directive = new DesplegarDirective();
    expect(directive).toBeTruthy();
  });
});
